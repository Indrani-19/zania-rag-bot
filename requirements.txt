fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
prometheus-client>=0.20.0
python-json-logger>=2.0.0

langchain-core>=0.3.0
langchain-text-splitters>=0.3.0
chromadb>=0.5.0
openai>=1.50.0
tiktoken>=0.8.0

pypdf>=5.0.0
openpyxl>=3.1.0

# Local embedding fallback for providers (Groq, OpenRouter) that don't host embeddings.
# Activated via EMBEDDING_PROVIDER=local. CPU-only; ~90 MB model on first call.
sentence-transformers>=3.0.0

# BM25 lexical retrieval. Fused with dense via RRF when HYBRID_ENABLED=true.
# Pure Python, ~50 KB. Index built lazily per document_id.
rank-bm25>=0.2.2
